import type { Incident } from '../types/incident';

export const getIncidents = async (): Promise<Incident[]> => {
  return [];
};

export const getIncidentById = async (_id: string): Promise<Incident | null> => {
  return null;
};

